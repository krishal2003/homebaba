import { Box, Container, Typography } from '@mui/material';
import React from 'react';

function Message() {
  return (
    <Container>
        <Box sx={{ backgroundColor:'white', color:'black'}}>

      <Typography>The Website is still under construction.</Typography>
        </Box>
    </Container>
  );
}

export default Message;
